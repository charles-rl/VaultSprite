# Python / uv
.venv/
__pycache__/
*.pyc
dist/
build/
*.egg-info/

# Logs & local run artifacts
logs/
crash/

# Local vault (memory writes during dev runs) — the app creates it at runtime
Vault/

# M3 stat persistence snapshot (local engine state; never committed)
state/
